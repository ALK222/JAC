# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET /commit          controllers.CommitController.getAll
GET /commit/:id      controllers.CommitController.get(id)
GET /commit/download controllers.CommitController.download